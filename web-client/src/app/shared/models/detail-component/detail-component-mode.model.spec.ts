import { DetailComponentModeModel } from "./detail-component-mode.model";

describe("DetailComponentMode", () => {
  it("should create an instance", () => {
    expect(new DetailComponentModeModel()).toBeTruthy();
  });
});
